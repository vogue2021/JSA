# 🚀 应用正确访问方式

## ⚠️ 重要提示

**✅ 问题已修复！后端CORS配置已更新。**

**请使用以下正确的URL访问应用：**

```
http://localhost:3002
```

## 🔧 问题原因及解决

**原因：**
- 前端服务器运行在 `http://localhost:3002`
- 但后端 `.env` 文件中 `FRONTEND_URL` 错误配置为 `http://localhost:3000`
- 导致CORS策略阻止了所有从 3002 来的请求

**已修复：**
- ✅ 已更新 `backend/.env` 文件，将 `FRONTEND_URL` 改为 `http://localhost:3002`
- ✅ 已重启后端服务器，新配置已生效
- ✅ CORS 现在正确允许来自 `http://localhost:3002` 的请求

之前您访问 `http://localhost:3000` 时出现的错误：
- ❌ "Failed to fetch" 错误
- ❌ 无法添加/删除学校
- ❌ 所有后端API调用失败

这些问题现在都已解决！

## ✅ 解决步骤

1. **关闭所有浏览器标签页**（特别是 localhost:3000 的标签）

2. **在浏览器中访问正确的地址：**
   ```
   http://localhost:3002
   ```

3. **测试学校添加功能：**
   - 以老师或管理员身份登录
   - 点击"学校"标签
   - 点击"添加学校"按钮
   - 填写学校信息并保存
   - 应该能成功添加（不再出现"Failed to fetch"错误）

## 📊 服务器状态

- ✅ 后端服务器：运行在 `http://localhost:3001`
- ✅ 前端服务器：运行在 `http://localhost:3002`
- ✅ CORS配置：允许 `http://localhost:3002` 的请求

## 🧪 测试账号

### 管理员
- 邮箱: admin@jsa.com
- 密码: admin123

### 老师
- 王老师: wang@school.com / wang123
- 李老师: li@school.com / li123

### 学生
- 张三: zhangsan@example.com / zhang123

## 📝 注意事项

如果浏览器自动跳转回 3000 端口，请：
1. 清除浏览器缓存
2. 在新的隐私/无痕窗口中打开 http://localhost:3002
3. 确保书签/收藏夹中保存的是 3002 端口的地址
